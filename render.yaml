services:
  # FastAPI backend on Render
  - type: web
    name: ppf-backend
    env: python
    rootDir: backend
    plan: free
    autoDeploy: true

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT

    envVars:
      # JWT / auth settings
      - key: SECRET_KEY
        generateValue: true
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "60"

      # Use SQLite file inside the service container (demo / non-persistent)
      - key: DATABASE_URL
        value: "sqlite+aiosqlite:///./rejections_dev.db"

  # React + Vite frontend on Render (static site)
  - type: web
    name: ppf-frontend
    rootDir: frontend
    plan: free
    autoDeploy: true

    # Static runtime tells Render this is a static site
    runtime: static

    buildCommand: |
      npm install
      npm run build

    # Path to built static files, relative to repo root (Blueprint: staticPublishPath)
    staticPublishPath: ./frontend/dist

    envVars:
      # Point frontend API to backend Render service URL (Blueprint: use envVarKey)
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: ppf-backend
          envVarKey: RENDER_EXTERNAL_URL

